name: data_pipeline
version: '1.0'
config-version: 2

profile: data_pipeline

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  data_pipeline:
    staging:
      +materialized: view
      +schema: dbt_schema
    marts:
      +materialized: table
      +schema: dbt_schema
